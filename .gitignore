.claude/

# Secrets / local env
.env
.env.*
!.env.example

# Node / TypeScript
node_modules/
dist/
*.tsbuildinfo
.npm/
coverage/

# OS
.DS_Store

# Local data / volumes / dbs
data/
*.db
*.sqlite
*.sqlite3
*.db-shm
*.db-wal

# Bench artifacts
bench/results/

# Python (legacy)
__pycache__/
*.pyc

# Local-only: the assignment brief + compliance self-assessment (kept on disk, not tracked)
ASSIGNMENT.md
COMPLIANCE.md

# Downloaded benchmark datasets (large; not tracked)
bench/data/

# Run logs (per-call LLM CSVs etc.) — folder kept, contents ignored
logs/*
!logs/.gitkeep
